import streamlit as st


def run_forecast_engine():
    st.title("Forecast Engine")
    st.subheader("Standalone forecast module")

    st.info(
        "Forecast logic currently lives inside the Timeline Engine. "
        "This page is reserved for the next standalone forecast build."
    )